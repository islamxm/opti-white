import {Carousel, Fancybox} from '@fancyapps/ui';

const objGallery = () => {
    const sliders = document.querySelectorAll('.objCard__img_slider');

    if(sliders.length > 0) {
        // sliders.forEach(i => {
        //     const cr = new Carousel(i, {
        //         Dots: false,
        //     })
            
        // })

        
    }
}

export default objGallery;